# Postgres
POSTGRES_DB=checkout
POSTGRES_USER=checkout
POSTGRES_PASSWORD=checkout

# Backend
ENV=development
JWT_SECRET=please-change-me
JWT_TTL_HOURS=24
ALLOWED_ORIGINS=*

# Public port for the nginx proxy. Everything else stays on the internal
# docker network — only the proxy is exposed to the host.
PROXY_PORT=80
