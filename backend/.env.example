# Server
PORT=8080
ENV=development

# Database (Postgres)
DATABASE_URL=postgres://postgres:postgres@localhost:5432/checkout?sslmode=disable

# JWT
JWT_SECRET=change-me-to-a-long-random-string
JWT_TTL_HOURS=24

# Seeded (upserted) password for admin@example.com on every boot.
ADMIN_PASSWORD=admin123

# CORS — comma separated origins
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:5174
